json.array! @buildings, partial: 'building', as: :building

<!--json.array! @buildings.each do |building|
  json.id building.id
  json.location building.location
  json.height building.height
end-->

